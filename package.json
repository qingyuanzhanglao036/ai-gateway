{
  "name": "ai-gateway",
  "version": "1.0.6",
  "description": "Cloudflare Workers AI API Gateway",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 3000 --ip 0.0.0.0",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "ai",
    "proxy",
    "api-gateway"
  ],
  "license": "Apache 2.0",
  "dependencies": {
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.107.0"
  }
}
